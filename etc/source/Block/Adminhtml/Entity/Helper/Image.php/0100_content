    /**
     * Get image preview url
     *
     * @return string
     */
    protected function {{_}}getUrl()
    {
        $url = false;
        if ($this->getValue()) {
            $url = $this->{{_}}imageModel->getBaseUrl().$this->getValue();
        }
        return $url;
    }
